import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.syedtahmidmanzoor.ossimulator',
  appName: 'OS Algorithm Simulator',
  webDir: 'dist'
};

export default config;
